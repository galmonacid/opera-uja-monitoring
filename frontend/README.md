# Frontend UJA Monitoring

SPA React/Vite del portal de monitorización.

## Comandos

```bash
npm install
npm run dev
npm run lint
npm test
npm run build
```

## Notas

- La app consume la API pública configurada en `VITE_API_BASE`.
- La suite de frontend usa `Vitest` + `Testing Library`.
